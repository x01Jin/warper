[package]
name = "warper"
version = "0.1.1"
description = "A Tauri App"
authors = ["x01Jin"]
license = "CC-BY-NC-SA-4.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "warper_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1.53.1", features = ["process", "time", "sync", "macros", "rt"] }
tauri-plugin-autostart = "2"
tauri-plugin-single-instance = "2"
tauri-plugin-store = "2"
tauri-plugin-notification = "2"
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls-manual-roots"] }
windows = { version = "0.62.2", features = [
  "Win32_System_JobObjects",
  "Win32_Foundation",
  "Win32_Security",
  "Win32_System_Threading",
  "Win32_System_Com",
  "Win32_System_Com_StructuredStorage",
  "Win32_System_Variant",
  "Win32_System_Registry",
  "Win32_UI_Shell",
  "Win32_UI_Shell_PropertiesSystem",
] }


# Read the optimization guideline for more details: https://tauri.app/concept/size/#cargo-configuration
[profile.release]
codegen-units = 1
lto = true
opt-level = 3
panic = "abort"
strip = true

# Fast iteration: keep panic backtraces useful, skip debuginfo for deps.
# Saves codegen + link time on every `tauri dev` / `tauri build --debug`.
[profile.dev]
debug = "line-tables-only"
incremental = true

[profile.dev.package."*"]
debug = false

# Fast shipped builds. Same manifest/behavior as
# release without the full LTO bill. Tauri CLI has no --profile flag, so use
# build-fast.ps1 (env overrides) or: cargo build --profile dev-release
[profile.dev-release]
inherits = "release"
opt-level = 2
codegen-units = 16
lto = false
debug = "line-tables-only"
incremental = true
strip = false
